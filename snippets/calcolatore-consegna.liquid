<p id="messaggio-consegna">24/48h</p>
<!--
<script>
  // Ottieni la data attuale
  let oggi = new Date();

  // Aggiungi 3 giorni
  oggi.setDate(oggi.getDate() + 3);

  // Calcola se il giorno è sabato (6) o domenica (0)
  if (oggi.getDay() === 6) {
    // Se è sabato, aggiungi altri 2 giorni
    oggi.setDate(oggi.getDate() + 2);
  } else if (oggi.getDay() === 0) {
    // Se è domenica, aggiungi 1 giorno
    oggi.setDate(oggi.getDate() + 1);
  }

  // Array per i giorni della settimana e i mesi
  const giorniSettimana = ['Domenica', 'Lunedì', 'Martedì', 'Mercoledì', 'Giovedì', 'Venerdì', 'Sabato'];
  const mesi = [
    'Gennaio', 'Febbraio', 'Marzo', 'Aprile', 'Maggio', 'Giugno',
    'Luglio', 'Agosto', 'Settembre', 'Ottobre', 'Novembre', 'Dicembre'
  ];

  // Formatta il messaggio
  let giornoSettimana = giorniSettimana[oggi.getDay()];
  let giorno = oggi.getDate();
  let mese = mesi[oggi.getMonth()];

  // Mostra il messaggio
  document.getElementById('messaggio-consegna').textContent = 
    `Compra ora e ricevilo entro ${giornoSettimana} ${giorno} ${mese}`;

  // Mostra il messaggio
  document.getElementById('messaggio-consegna').textContent = 
    `Compra ora e ricevilo entro ${giornoSettimana} ${giorno} ${mese}`;
</script>-->
